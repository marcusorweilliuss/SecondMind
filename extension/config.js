// Cortex extension configuration.
// Edit these to point at your deployment + Supabase project, then reload the
// extension in chrome://extensions.
self.CORTEX_CONFIG = {
  // Base URL of your Next.js app (no trailing slash).
  API_BASE: "http://localhost:3000",
  // Supabase project URL + anon key (same values as NEXT_PUBLIC_* in .env.local).
  SUPABASE_URL: "https://your-project.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-key",
};
